{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "install-all": "composer install && npm install",
        "setup": "composer install && npm install && copy .env.example .env && php artisan key:generate && composer require livewire/livewire",
        "serve": "start \"Laravel Server\" php artisan serve && start \"Vite Dev\" npm run dev",
        "optimize": "npm run build && php artisan optimize"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.21",
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "postcss": "^8.5.6",
        "tailwindcss": "^3.4.17",
        "vite": "^5.0.0"
    }
}